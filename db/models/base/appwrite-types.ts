export enum AttributeType {
  STRING = 'STRING',
  INTEGER = 'INTEGER',
  FLOAT = 'FLOAT',
  BOOLEAN = 'BOOLEAN',
  IP = 'IP',
  URL = 'URL',
  EMAIL = 'EMAIL',
  ENUM = 'ENUM',
}

export enum IndexType {
  KEY = 'key',
  UNIQUE = 'unique',
  FULL_TEXT = 'fulltext',
}

export enum IndexOrder {
  ASC = 'ASC',
  DESC = 'DESC',
}

